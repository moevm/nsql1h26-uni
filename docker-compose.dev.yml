services:
  fastapi-app:
    volumes:
      - ./backend/app:/app/app
    command: sh -c "sleep 5 && python init_collections.py && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev
